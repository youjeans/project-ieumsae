<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>이음새</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <% if (id) { %>
        <header>
            <img src="/img/logo.png" alt="로고" class="logo">
            <button class="notification-btn" onclick="navigateToAlarm()">
                <img src="/img/mailbox.png" alt="알림 아이콘" class="notification-icon">
                <span class="notification-count"><%= count %></span>
            </button>
            <script>
                function navigateToAlarm() {
                    window.location.href = "/diaryAlarm";
                }
            </script>
            <button class="btn" onclick="navigateToLogout()">로그아웃</button>
            <script>
                function navigateToLogout() {
                    window.location.href = "/login/logout";
                }
            </script>
            <button class="btn" onclick="navigateToMypage()">마이페이지</button>
            <script>
                function navigateToMypage() {
                    window.location.href = "/mypage";
                }
            </script>
            <button class="btn">마이페이지</button>
        </header>
        <div class="container">
            <div class="profile-section">
                <img src="/img/default-profile" alt="프로필 사진" class="profile-pic">
                <h2><span id="username"><%= name %></span>님의 일기장</h2>
                <button class="btn" onclick="navigateToList()">친구 관리</button>
                <script>
                    function navigateToList() {
                        window.location.href = "/list";
                    }
                </script>
                <button class="btn" onclick="navigateToWriteDiary()">일기 쓰기</button>
                <script>
                    function navigateToWriteDiary() {
                        window.location.href = "/diary/form";
                    }
                </script>
            </div>
    
            <div class="diary-section">
                <div class="diary-container">
                    <div class="diary">
                        <img src="/img/default-diary.png" alt="일기 노트" class="diary-img" id="diaryImage1" onclick="navigateToMy();">
                        <div class="diary-name">
                            <p><%= myname %></p>
                        </div>
                    </div>
                    <% exchangePartners.forEach((partner) => { %>
                        <div class="diary">
                            <img src="/img/default-diary.png" alt="일기 노트" class="diary-img" id="diaryImage2" onclick="navigateToPartner(<%= partner.회원번호 %>);">
                            <div class="diary-name">
                                <p><%= partner.이름 %></p>
                            </div>       
                        </div>
                    <% }); %>
                    <script>
                        function navigateToMy() {
                            window.location.href = "/diaryPage/my";
                        }
                        function navigateToPartner(partnerId) {
                            window.location.href = "/diaryPage/" + partnerId;
                        }
                    </script>
                </div>
            </div>
        </div>
    <% } else { %>
        <header>
            <img src="/img/logo.png" alt="로고" class="logo">
            <button class="notification-btn">
                <img src="/img/mailbox.png" alt="알림 아이콘" class="notification-icon">
            </button>
            <button class="btn" onclick="navigateToLogin()">로그인</button>
            <script>
                function navigateToLogin() {
                    window.location.href = "/login";
                }
            </script>
        </header>
        <div class="container">
            <div class="profile-section">
                <img src="/img/default-profile.png" alt="프로필 사진" class="profile-pic">
                <h2>로그인이 필요합니다.</h2>
                <button class="btn" onclick="navigateToSignUp()">회원가입</button>
                <script>
                    function navigateToSignUp() {
                        window.location.href = "/signup";
                    }
                </script>
                <button class="btn" onclick="navigateToLogin()">로그인</button>
                <script>
                    function navigateToLogin() {
                        window.location.href = "/login";
                    }
                </script>
            </div>
    
            <div class="diary-section">
                <div class="diary-container">
                    <div class="diary">
                        <img src="/img/default-diary.png" alt="일기 노트" class="diary-img" id="diaryImage1">       
                    </div>
                    <div class="diary">
                        <img src="/img/default-diary.png" alt="일기 노트" class="diary-img" id="diaryImage2">       
                    </div>
                    <div class="diary">
                        <img src="/img/default-diary.png" alt="일기 노트" class="diary-img" id="diaryImage3">       
                    </div>
                </div>
            </div>
        </div>
    <% } %>

    <script src="/js/script.js"></script>
</body>
</html>
