<!--diaryPage.ejs-->
<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>일기 페이지</title>
    <style>
        @font-face {
            font-family: 'KOTRAHOPE';
            src: url('https://fastly.jsdelivr.net/gh/projectnoonnu/noonfonts_2110@1.0/KOTRAHOPE.woff2') format('woff2');
            font-weight: normal;
            font-style: normal;
        }
        body {
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
            background-color: #fac4cf;
        }
        .btn {
            display: inline-block;
            margin: 10px 10px;
            padding: 10px 20px;
            font-size: 1rem;
            color: #e97da4;
            background-color: #ffffff;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: background-color 0.5s ease;
        }
        .btn:hover {
            background-color: #e97da4;
            color: white;
        }
        .logo {
            position: absolute;
            top: 10%;
            left: 10%;
            width: 200px;
            height: auto;
            z-index: 999;
        }
        header {
            position: absolute;
            top: 0;
            left: 0;
            width: 90%;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            padding: 20px;
            box-sizing: border-box;
            z-index: 999;
        }
        .diary-container {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            position: relative;
            width: 700px;
            height: 500px;
            background-color: rgb(255, 255, 255);
            border-radius: 40px;
            padding: 30px;
            box-sizing: border-box;
            overflow: auto;
            font-family: KOTRAHOPE, sans-serif;
        }
        p {
            color: #e97da4;
        }
        .divider {
            height: 2px;
            width: 640px;
            background-color: #ccc;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <header>
        <img src="/img/logo.png" alt="로고" onclick="navigateToMain()" class="logo">
        <script>
            function navigateToMain() {
                window.location.href = "/";
            }
        </script>
        <button class="btn" onclick="navigateToMain()">홈</button>
        <script>
            function navigateToMain() {
                window.location.href = "/";
            }
        </script>
        <button class="btn" onclick="navigateToLogout()">로그아웃</button>
        <script>
            function navigateToLogout() {
                window.location.href = "/login/logout";
            }
        </script>
        <button class="btn" onclick="navigateToMyPage()">마이페이지</button>
        <script>
            function navigateToMyPage() {
                window.location.href = "/mypage";
            }
        </script>
    </header>
    <div class="diary-container">
        <div class="reply-button-container" style="display: flex; justify-content: flex-end; margin-bottom: 20px;">
            <form action="/diary/reply" method="get">
                <input type="hidden" name="exchange" value="<%= exchange %>">
                <input type="hidden" name="exchangeType" value="<%= exchangeType %>">
                <button type="submit" class="btn">답장하기</button>
            </form>
        </div>
        <% result.map(function(diary) { %>
            <p class="date"><%= diary.일기_송신일 %></p>
            <p class="send">작성자: <%= diary.작성자_이름 %></p>
            <section>
                <%= diary.일기_내용 %>
                <div class="divider"></div>
            </section>
        <% }); %>
    </div>
</body>
</html>
