<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>일기 알람</title>
    <style>
        body {
      font-family: Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
      background-color: #fac4cf;
    }
    .btn {
        display: inline-block;
        margin: 10px 10px;
        padding: 10px 20px;
        font-size: 1rem;
        color: #e97da4;
        background-color: #ffffff;
        border: none;
        border-radius: 25px;
        cursor: pointer;

        transition: background-color 0.5s ease;
    }

    .btn:hover {
        background-color: #e97da4; /* 호버 시 배경색 변경 */
        color: white;
    }
    .logo {
            position: absolute;
            top: 10%;
            left: 10%;
            width: 200px;
            height: auto;
            z-index: 999;
        }

        header {
            position: absolute;
            top: 0;
            left: 0;
            width: 90%;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            padding: 20px;
            box-sizing: border-box;
            z-index: 999;
        }
        .diary-container {
          display: flex; /* Flex 컨테이너로 설정 */
          flex-direction: column; /* 요소들을 세로로 정렬 */
          align-items: flex-start;
          position: relative;
          width: 500px; /* 가장 큰 사각형의 너비 */
          height: 500px; /* 모든 사각형의 높이 */
          background-color: rgb(255, 255, 255);
          border-radius: 40px;
          padding: 30px; /* 내부 여백 */
          box-sizing: border-box; /* 패딩을 포함한 전체 너비 계산 */
          overflow: auto;
        }
        p {
            color: #e97da4;
        }
        .o{
            color: #6d6d6d;
        }
        .divider {
            height: 2px; /* 선의 두께 */
            width: 440px;
            background-color: #ccc; /* 선의 색상 */
            margin: 20px 0; /* 위아래 마진 추가 */
        }
        .open{
            color: #e97da4;
        }
        .open:hover{
            color: deeppink;
        }



    </style>
</head>
<body>
    <header>
        <img src="/img/logo.png" alt="로고" onclick="navigateToMain()" class="logo">
        <script>
            function navigateToMain() {
                window.location.href = "/";
            }
        </script>
        
        <button class="btn" onclick="navigateToMain()">홈</button>
        <script>
            function navigateToMain() {
                window.location.href = "/";
            }
        </script>
         <button class="btn" onclick="navigateToLogout()">로그아웃</button>
         <script>
             function navigateToLogout() {
                 window.location.href = "/login/logout";
             }
         </script>
          <button class="btn" onclick="navigateToMyPage()">마이페이지</button>
          <script>
              function navigateToMyPage() {
                  window.location.href = "/mypage";
              }
          </script>
    </header>
    <div class="diary-container"> 
    <% result.map(function(alarm) { %>
        <section>
            <div><p>♡ <%= alarm.송신자_이름 %>님에게서 일기가 도착했습니다! 지금 바로 확인해 보세요.</p><div>
            <div><p class="o">도착 일시: <%= alarm.수신일시 %></p></div>
            <a class="open" href="/diaryAlarm/<%= alarm.교환기록_번호 %>">click > OPEN!<a>
            <div class="divider"></div>
        <section>
    <% }); %>
</div>


</body>
</html>